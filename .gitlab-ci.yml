image: python:latest

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

cache:
  paths:
    - .cache/pip
    - venv/

before_script:
  - python --version  # For debugging
  - pip install virtualenv
  - virtualenv venv
  - source venv/bin/activate
  - pip install -e .[dev]

stages: 
  - test_low_api
  - test_hi_api

test-lo:
  stage: test_low_api
  script:
    - pytest tests/test_reg_lo_api.py

test-hi:
  stage: test_hi_api
  script:
    - pytest tests/test_reg_hi_api_seed.py
